import { AcceptedLinkFormatDirective } from './accepted-link-format.directive';

describe('AcceptedLinkFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AcceptedLinkFormatDirective();
    expect(directive).toBeTruthy();
  });
});
